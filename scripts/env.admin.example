# Copy to repo root as .env.admin (gitignored). Used only by npm run admin:* scripts.
# Never use the service role in Vite (VITE_*) — it ships in the browser bundle.

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_here
